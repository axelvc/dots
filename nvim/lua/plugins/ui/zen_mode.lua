require('zen-mode').setup {
  window = {
    backdrop = 1,
    options = {
      number = true,
      relativenumber = true,
    },
  },
  -- on_open = function (win)
  --   vim.opt_local.scrolloff = 100
  -- end
}
